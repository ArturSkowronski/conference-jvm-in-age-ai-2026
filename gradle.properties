org.gradle.caching=true
org.gradle.parallel=true

# Default Java toolchain; override with: -PjavaVersion=23
javaVersion=25

# For FFM downcalls (TensorFlow C API) at runtime.
# You can also pass this manually via: `./gradlew :demos:tensorflow-ffm:run --enable-native-access=ALL-UNNAMED`
