# JCuda support demo

This demo prints basic CUDA driver + device information via **JCuda**.

## Prereqs

- NVIDIA GPU + installed CUDA **driver** (on Linux/Windows, `nvidia-smi` should work)
- A supported OS/arch combination for JCuda native binaries

## Run

From repo root:

- `./gradlew :demos:jcuda:run`
